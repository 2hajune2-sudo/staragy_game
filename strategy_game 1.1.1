import random
import keyboard

stert = 0
random_monster = 0
monster_list = {
    '용암 슬라임' : [(2,3,2), '뜨거운 슬라임 입니다,영역을 넓이며 공간이 충분하면 분열하고, 물리적인 공격이 더 불어나는 듯 합니다'],
    '푸른 드레곤' : [(1,2,3), '푸른 드레곤 입니다,주로 사람이 많은 곳에 브레스를 쏘며 피해를 주고 있습니다,확실한 약점이 있는듯 합니다'],
    '칼날늑대': [(1,2,3), '날카로운 늑대입니다,영역을 넓이며 많은 사람이 빠질 큰함정을 파고 협공하여 피해를 주고 있습니다,마법에 무효인 개채를 상당수 감지하였습니다'],
    '차원 강아지':[(3,3,2), '귀여운 강아지 입니다,습성과 행동은 강아지와 유사하며,벽을 통과하며 소란을 피워 피해를 주고 있습니다,약 50마리가 존재 합니다'],
    '강철 골램':[(1,3,2), '묵직한 고철 골램입니다,행동이 둔하지만 강한 공격으로 피해를 주고 있습니다,전기나,물로도 녹이슬게하거나,움직임이 멈추지 않는걸로 확인됩니다'],
    '점토 병사':[(1,3,1), '성인 남성 평균보다 조금작은 점토 병사 입니다,일정 시간이 지나면 분열하기도 하며,일정 피해를 받을시 사라집니다,여러 피해를 주고있습니다,왜인지 몰라도 방수입니다'],
    '스핑크스?':[(3,1,3), '스핑크스로 추정되는 모형입니다, 사람이 있다면 문제를 내며 틀릴시 폭주하며 피해를 주고 있습니다, 근처의 모든 대상에 게 한 문제를 내며 사람이 많으면 어려운 문제를 냅니다, 100번의 문재를 맞추면 소멸하는것으로 확인되며,부서지지 않습니다.'],
    '마법 크리스탈':[(2,1,3), '좀 큰 크리스탈 입니다,신비한 에너지를 뿜으며 주변 공기와 사람들에게 힘을 대량으로 얻어서 폭주중 입니다,물리적인 공격은 튕겨내고 있습니다.']
    }

moster = ''
unit = 1 #부대 종류 (1 : 군대,2 : 마법사,3 : 전략가)
many = 2 #많고, 적음 (1 : 소,2 : 중,3 : 대)
s_y = 1 #전략(stacstrategy), (1 : 공격적,2 : 수비적,3 : 탐구적)
sistem = 1 #설정 대상 (1 : 부대 종류,2 : 많고,적음,3 : 전략)

#======================================================================================================================================

random_monster, strategy_and_what = random.choice(list(monster_list.items()))
strategy, what = strategy_and_what

print('당신은 평범하지 않지않지아니하지아니하지않은 회사에서')
print('특별 않지않지아니하지아니한 일을 하는 사람 입니다.')
print('여기서 당신은 합리적이고,지니어스하고,객관적이고,스마트한 선택을 내리세요')
print('(위 화살표로 시작)')
def new_monster():
    random_monster, strategy_and_what = random.choice(list(monster_list.items()))
    strategy, what = strategy_and_what
    print('tip: 군대: 주로 화기(총)을 사용,마법사:물,불,전기,풀 속성 공격 가능,전략가:머리 좋음,괴물보단 이상현상에 유용')
    print('')
    print('---------------------------------------------------------------')
    print('이상현상:',random_monster)
    print(what)
def r_up(event):
    global unit
    global many
    global s_y
    if stert == 1:
        if sistem == 1:
            if unit == 3:
                unit = 1
            else:
                unit += 1
            print('현재 부대 :')
            if unit == 1:
                print('군대')
            elif unit == 2:
                print('마법사')
            else:
                print('전략가')
        elif sistem == 2:
            if many == 3:
                many = 1
            else:
                many += 1
            print('현재 전력 :')
            if many == 1:
                print('소')
            elif many == 2:
                print('중')
            else:
                print('대')
        else:
            if s_y == 3:
                s_y = 1
            else:
                s_y += 1
            print('현재 전략 :')
            if s_y == 1:
                print('공격적')
            elif s_y == 2:
                print('수비적')
            else:
                print('탐구적')
    else:
        pass

def l_down(event):
    global unit
    global many
    global s_y
    if stert == 1:
        if sistem == 1:
            if unit == 1:
                unit = 3
            else:
                unit -= 1
            print('현재 부대 :')
            if unit == 1:
                print('군대')
            elif unit == 2:
                print('마법사')
            else:
                print('전략가')
        elif sistem == 2:
            if many == 1:
                many = 3
            else:
                many -= 1
            print('현재 전력 :')
            if many == 1:
                print('소')
            elif many == 2:
                print('중')
            else:
                print('대')
        else:
            if s_y == 1:
                s_y = 3
            else:
                s_y -= 1
            print('현재 전략 :')
            if s_y == 1:
                print('공격적')
            elif s_y == 2:
                print('수비적')
            else:
                print('탐구적')
    else:
        pass

def u_system(event):
    global stert, sistem
    if stert == 1:
        if sistem == 3:
            sistem = 1
            print('현재 부대 설정중')
        else:
            sistem += 1
            if sistem == 2:
                print('현재 전력 설정중')
            else:
                print('현재 전략 설정중')
    else:
        global random_monster
        stert = 1
        print('게임 설명 =================================================================================================================')
        print('플레이 방법 : (위 화살표 : 부대의 종류,병력 수,전략을 선택)\n(오른쪽 화살표 : 다음 선택)\n(왼쪽 화살표 : 이전 선택)\n(아래 화살표 : 전략 개시!)\n미션을 보고 해결 하세요,모든 선택지를 보시고 고르는걸 추천 드립니다.')
        print('tip: 군대: 주로 화기(총)을 사용,마법사:물,불,전기,풀 속성 공격 가능,전략가:머리 좋음,괴물보단 이상현상에 유용')
        print('')
        print('')
        print('이상현상:',random_monster)
        print(what)

def d_dayend(event):
    if stert == 1:
        success = 0
        if strategy[0] == unit:
            success += 1
        if strategy[1] == many:
            success += 1
        if strategy[2] == s_y:
            success += 1
        if success == 3:
            print('')
            print('')
            print('')
            print('작전 결과:')
            print('완벽한 전략이었습니다!')
        elif success != 0:
            print('')
            print('')
            print('')
            print('작전 결과:')
            print('전략에 실수가 있었나 봅니다...')
        else:
            answser = random.random()
            print('')
            print('')
            print('')
            if answser < 0.25:
                print('사람은 누구나 그럴듯한 계획이있다.. 처맞기 전까진')
            elif answser < 0.5:
                print('맞는 전략이에요!..처맞는')
            elif answser < 0.75:
                print('저희 고양이가 더 잘하겠네요,그치 야옹아? *야옹*')
            elif answser < 0.99:
                print('**허                                      접      ㅋ**')
            else:
                print('이 메시지는 극악의 확률과 극악의 실력으로 나오는 메시지로, 당신이 *곰팡이핀 에어컨*과 동급이란 뜻 입니다!!')
        new_monster()        






#========================================================================================================================================

keyboard.on_press_key("Right", r_up)
keyboard.on_press_key("Left", l_down)
keyboard.on_press_key("Up", u_system)
keyboard.on_press_key("Down", d_dayend)

keyboard.wait()
